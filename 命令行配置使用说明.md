# Beszel 命令行配置使用说明

## 🚀 快速开始

### 一键配置脚本

已创建自动配置脚本 `configure_beszel.sh`，一键完成所有配置：

```bash
cd /Users/wzy/projects/08-Beszel
bash configure_beszel.sh
```

脚本会自动完成：
- ✅ 登录 Beszel
- ✅ 配置钉钉告警通知
- ✅ 配置邮件通知
- ✅ 创建 CPU/内存/磁盘告警规则
- ✅ 显示配置状态
- ✅ 显示使用说明

---

## 📖 手动配置命令

### 1. 获取 Token

```bash
curl -s -X POST http://localhost:38005/api/collections/users/auth-with-password \
  -H "Content-Type: application/json" \
  -d '{"identity":"442333521@qq.com","password":"!Wangzeyu66!@#"}' | jq -r '.token' > /tmp/beszel_token.txt

TOKEN=$(cat /tmp/beszel_token.txt)
```

### 2. 配置通知

```bash
# 获取用户设置 ID
SETTINGS_ID=$(curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:38005/api/collections/user_settings/records | jq -r '.items[0].id')

# 更新用户设置（邮件 + 钉钉）
curl -s -X PATCH "http://localhost:38005/api/collections/user_settings/records/$SETTINGS_ID" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "settings": {
      "chartTime": "1h",
      "emails": ["442333521@qq.com"],
      "webhooks": [
        "generic://oapi.dingtalk.com/robot/send?access_token=7d37088634657d310642583a6f93e0da1594ab6134beff8589caa2690031d539&template=json&$msgtype=text"
      ]
    }
  }' | jq .
```

### 3. 添加服务器

```bash
# 添加本地服务器
curl -s -X POST http://localhost:38005/api/collections/systems/records \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "本地服务器",
    "host": "localhost",
    "port": "45876",
    "status": "up",
    "users": ["oafuupsh0vbbr4k"]
  }' | jq .

# 获取系统 ID
SYSTEM_ID=$(curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:38005/api/collections/systems/records | jq -r '.items[0].id')
```

### 4. 创建告警规则

```bash
# CPU 告警（80%，持续 10 分钟）
curl -s -X POST http://localhost:38005/api/beszel/user-alerts \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d "{
    \"name\": \"CPU\",
    \"value\": 80,
    \"min\": 10,
    \"systems\": [\"$SYSTEM_ID\"],
    \"overwrite\": true
  }" | jq .

# 内存告警（90%，持续 5 分钟）
curl -s -X POST http://localhost:38005/api/beszel/user-alerts \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d "{
    \"name\": \"Memory\",
    \"value\": 90,
    \"min\": 5,
    \"systems\": [\"$SYSTEM_ID\"],
    \"overwrite\": true
  }" | jq .

# 磁盘告警（85%，立即触发）
curl -s -X POST http://localhost:38005/api/beszel/user-alerts \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d "{
    \"name\": \"Disk\",
    \"value\": 85,
    \"min\": 0,
    \"systems\": [\"$SYSTEM_ID\"],
    \"overwrite\": true
  }" | jq .
```

### 5. 查看配置状态

```bash
# 查看用户设置
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:38005/api/collections/user_settings/records | jq '.items[0].settings'

# 查看所有系统
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:38005/api/collections/systems/records | jq .

# 查看所有告警
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:38005/api/collections/alerts/records | jq .
```

---

## 🖥️ 添加服务器

### 方式一：Web 界面

1. 访问 http://localhost:38005
2. 点击右上角 **"Add System"**
3. 填写服务器信息：
   - **System Name**: 服务器名称
   - **Host/IP**: 服务器 IP 地址
   - **SSH Port**: SSH 端口（默认 22）
4. 点击 **"Add System"**
5. 复制显示的安装命令
6. 在目标服务器执行安装命令

### 方式二：使用 SSH

```bash
# 1. 使用 WinSCP 或 SSH 连接到目标服务器
# 2. 将本地公钥添加到目标服务器的 authorized_keys
cat /Users/wzy/projects/08-Beszel/keys/beszel_agent.pub >> ~/.ssh/authorized_keys

# 3. 在 Web 界面添加服务器
# Host/IP: 填写目标服务器 IP
# SSH Port: 默认 22
# 点击 "Add System"
```

### 方式三：Docker Compose（推荐）

在目标服务器上创建 `docker-compose.yml`：

```yaml
services:
  beszel-agent:
    image: henrygd/beszel-agent:latest
    container_name: beszel-agent
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./beszel_agent_data:/var/lib/beszel-agent
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      HUB_URL: http://your-hub-ip:38005
      TOKEN: <token-from-web-ui>
      KEY: "<public-key-from-web-ui>"
```

启动 Agent：

```bash
docker compose up -d
```

---

## 🔧 常用管理命令

```bash
cd /Users/wzy/projects/08-Beszel

# 查看容器状态
docker compose ps

# 查看日志
docker compose logs -f beszel         # Hub 服务
docker compose logs -f beszel-agent   # Agent 服务

# 重启服务
docker compose restart

# 重启单个服务
docker compose restart beszel
docker compose restart beszel-agent

# 停止服务
docker compose down

# 停止并删除数据
docker compose down -v

# 启动服务
docker compose up -d

# 更新到最新版本
docker compose pull
docker compose up -d

# 查看资源占用
docker stats beszel beszel-agent

# 查看容器详情
docker inspect beszel
docker inspect beszel-agent
```

---

## 📊 配置信息

### 访问信息
- **URL**: http://localhost:38005
- **用户名**: 442333521@qq.com
- **密码**: !Wangzeyu66!@#

### 钉钉告警
- **Webhook URL**: https://oapi.dingtalk.com/robot/send?access_token=7d37088634657d310642583a6f93e0da1594ab6134beff8589caa2690031d539
- **加签密钥**: SECe4bda9a71c04798d20fc759cc7839c1a3b6a86bf8bd9977b8ef38d20a35e393c
- **Robot Code**: ding0re41bn9yex7xvwk

### 邮件通知
- **发件人**: qwzy@foxmail.com
- **收件人**: 442333521@qq.com
- **SMTP 服务器**: smtp.qq.com
- **端口**: 587
- **授权码**: wlxqztvxcreqebei

### SSH 密钥
- **私钥**: /Users/wzy/projects/08-Beszel/keys/beszel_agent
- **公钥**: /Users/wzy/projects/08-Beszel/keys/beszel_agent.pub

### 已配置的服务器
- **本地服务器**: vpyadqj5zavicfm (localhost:45876)

### 已配置的告警
- ✅ CPU 使用率 > 80% (持续 10 分钟)
- ✅ 内存使用率 > 90% (持续 5 分钟)
- ✅ 磁盘使用率 > 85% (立即触发)

---

## 🔌 API 端点参考

### 认证
```bash
POST /api/collections/users/auth-with-password
Content-Type: application/json
{
  "identity": "442333521@qq.com",
  "password": "!Wangzeyu66!@#"
}
```

### 用户设置
```bash
GET    /api/collections/user_settings/records
PATCH  /api/collections/user_settings/records/{id}
POST   /api/collections/user_settings/records
```

### 系统管理
```bash
GET    /api/collections/systems/records
POST   /api/collections/systems/records
DELETE /api/collections/systems/records/{id}
```

### 告警规则
```bash
GET    /api/collections/alerts/records
POST   /api/beszel/user-alerts
```

### 容器监控
```bash
GET /api/beszel/containers/{systemId}
```

---

## 🛠️ 故障排查

### 容器无法启动
```bash
# 查看日志
docker compose logs beszel

# 检查端口占用
lsof -i :38005

# 检查磁盘空间
df -h
```

### Agent 无法连接到 Hub
```bash
# 检查网络连接
ping hub-ip-address

# 查看 Agent 日志
docker logs beszel-agent

# 检查防火墙
sudo ufw status
```

### 告警未发送
```bash
# 检查通知配置
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:38005/api/collections/user_settings/records | jq .

# 测试钉钉 Webhook
curl -X POST 'https://oapi.dingtalk.com/robot/send?access_token=7d37088634657d310642583a6f93e0da1594ab6134beff8589caa2690031d539' \
  -H 'Content-Type: application/json' \
  -d '{"msgtype":"text","text":{"content":"测试消息"}}'
```

### 数据库问题
```bash
# 检查数据库文件
ls -la /Users/wzy/projects/08-Beszel/beszel_data/data.db

# 查看数据库表
sqlite3 /Users/wzy/projects/08-Beszel/beszel_data/data.db ".tables"

# 查看系统数据
sqlite3 /Users/wzy/projects/08-Beszel/beszel_data/data.db "SELECT * FROM systems"
```

---

## 📁 文件清单

```
/Users/wzy/projects/08-Beszel/
├── docker-compose.yml              # Docker Compose 配置
├── configure_beszel.sh             # 自动配置脚本
├── Beszel完整配置文档.md           # 完整配置文档
├── 命令行配置使用说明.md           # 本文档
├── keys/                           # SSH 密钥
│   ├── beszel_agent               # 私钥
│   └── beszel_agent.pub           # 公钥
├── beszel_data/                   # Hub 数据目录
│   └── data.db                    # 数据库文件
└── beszel_agent_data/             # Agent 数据目录
```

---

## 📞 支持与帮助

- **项目主页**: https://github.com/henrygd/beszel
- **官方文档**: https://beszel.dev
- **API 文档**: https://beszel.dev/guide/rest-api
- **问题反馈**: https://github.com/henrygd/beszel/issues

---

## 📌 快速参考

### 访问地址
- **Web 界面**: http://localhost:38005
- **用户名**: 442333521@qq.com
- **密码**: !Wangzeyu66!@#

### 常用命令
```bash
cd /Users/wzy/projects/08-Beszel

# 查看状态
docker compose ps

# 查看日志
docker compose logs -f

# 重启服务
docker compose restart

# 自动配置
bash configure_beszel.sh
```

### 已配置的服务器
- **本地服务器**: vpyadqj5zavicfm (localhost:45876)

### 已配置的告警
- ✅ CPU 使用率 > 80% (10 分钟)
- ✅ 内存使用率 > 90% (5 分钟)
- ✅ 磁盘使用率 > 85% (立即)

### 已配置的通知
- ✅ 钉钉 Webhook
- ✅ 邮件通知 (442333521@qq.com)

---

**文档最后更新**: 2026-02-01
**Beszel 版本**: 0.18.2
**配置工具版本**: v1.0