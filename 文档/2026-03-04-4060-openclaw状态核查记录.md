# 2026-03-04 4060 openclaw 状态核查记录

## planning
- 对应任务：`planning/07-20260304-4060-openclaw运行状态核查/`
- 查重结论：历史任务最高相似度 73%，不满足复用阈值（>=80%），已新建任务目录并执行。

## implementation
- 本次无代码实现变更，执行的是运行态核验与诊断。
- 检查路径：
  1. 连接性确认：`192.168.192.10` 不通，`192.168.193.10` 可达。
  2. SSH 登录：`ssh -i ~/.ssh/id_rsa_github -p 35622 wzy@192.168.193.10`
  3. 版本核验：`openclaw --version` -> `2026.3.2`
  4. 进程核验：`pgrep -af openclaw`（主进程 `openclaw-gateway`）
  5. 端口核验：`ss -lntp`（`18789/18791/18792` 监听）
  6. 可用性核验：`curl http://192.168.193.10:18789/` -> `HTTP 200`
  7. 日志核验：`journalctl --user -u openclaw-gateway --since \"2 hours ago\"`

## completion
- 结论：`4060` 上 `openclaw` 当前可用。
- 证据：
  - 网关进程持续运行（非 Docker 形态）。
  - 对外入口 `http://192.168.193.10:18789/` 可正常返回。
  - 本地接口 `127.0.0.1:18792` 返回 `OK`，`127.0.0.1:18791` 需要授权（`401`）。
- 发现的非阻塞异常：
  - `win-cli` SSH MCP 工具在本次会话不可用（响应解码错误）。
  - 网关日志存在 `chatgpt.com` DNS 解析失败导致的 embedded agent `error=500`，但未造成网关服务不可用。

## project_summary
- 本项目用于运维和监控相关能力建设（Beszel 与周边自动化），本次任务属于对 `4060` 节点上 OpenClaw 网关运行状态的现场核验。

## usage_guide
- 日常快速检查（建议）：
  1. `ssh -i ~/.ssh/id_rsa_github -p 35622 wzy@192.168.193.10`
  2. `openclaw --version`
  3. `pgrep -af openclaw`
  4. `ss -lntp | rg '18789|18791|18792'`
  5. `curl -I http://192.168.193.10:18789/`
  6. `journalctl --user -u openclaw-gateway -n 100 --no-pager`
- 判定建议：
  - 进程存在 + 18789 可达 + 接口返回 200 + 无持续 fatal/error，判定为正常。
