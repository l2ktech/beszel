# 2026-03-04：仅193延迟采集与曲线图

## planning
- 复用任务：`planning/05-20260303-192193网络波动掉线采集告警/`
- 复用判定：相似度 95%，不新建目录。
- 目标：
  1. 回答“项目是否正常、数据是否更新”。
  2. 去掉 192 延迟测试，仅保留 193。
  3. 将 193 延迟接入可曲线展示的数据链路。

## implementation
### 1) 运行与故障定位
- 容器与接口可用：`docker compose ps`、`curl http://127.0.0.1:38005/api/health` 正常。
- 发现采集异常：`zt_latency_sync.sh` 日志反复出现 `${1,,}: bad substitution`，脚本在告警分支中断。

### 2) 脚本改造（`scripts/zt_latency_sync.sh`）
- 修复 Bash 兼容：`is_true()` 改为 `tr` 小写转换，兼容 macOS Bash 3.2。
- 仅测 193：
  - 探测函数改为 `derive_target_193`。
  - 停止 192 探测与 192 告警触发。
  - `z192_status` 固化为 `disabled`。
- 新增图表时序写入：
  - 每次采样写入 `system_stats` 新类型 `zt1m`。
  - 采集字段：`stats.z193l`（延迟）、`stats.z193j`（抖动）、`stats.z193s`（状态）。

### 3) 前端改造
- 文件：`internal/site/src/components/routes/system.tsx`、`internal/site/src/types.d.ts`。
- 增加 `ZT 193 Latency` 图表卡片。
- 图表数据源改为读取 `system_stats.type='zt1m'`，避免影响原 CPU/内存等图。

## completion
- 脚本校验：`bash -n scripts/zt_latency_sync.sh` 通过。
- 脚本执行：单次运行成功，日志显示 `sync done scanned=14`。
- 数据落库：
  - `system_stats` 出现 `zt1m` 类型。
  - 70 秒复测：`zt1m` 计数 `56 -> 70`，`zt_probe_ts` 持续递增。
- 前端构建：`cd internal/site && npm run -s build` 通过。
- 部署生效：
  - `docker build -f internal/dockerfile_hub -t beszel:zt-latency-email .`
  - `docker compose up -d beszel`
  - `curl http://127.0.0.1:38005/api/health` 返回 `200`。

## project_summary
- 本项目是 Beszel 监控系统的本地定制版本。
- 除原生系统资源监控外，扩展了 ZeroTier 网络延迟采集与告警。

## usage_guide
### 入口
- Hub：`http://127.0.0.1:38005/`
- 健康检查：`http://127.0.0.1:38005/api/health`

### 常用命令
- 查看容器：`docker compose ps`
- 手动执行一次采集：`./scripts/zt_latency_sync.sh`
- 查看采集日志：`tail -f ~/Library/Logs/com.wzy.beszel.zt-latency-sync.log`
- 查看曲线数据：
  - `sqlite3 beszel_data/data.db "SELECT type, COUNT(*) FROM system_stats GROUP BY type;"`
  - `sqlite3 beszel_data/data.db "SELECT created, json_extract(stats,'$.z193l') FROM system_stats WHERE type='zt1m' ORDER BY created DESC LIMIT 20;"`

### 依赖
- 前端构建依赖 Node.js + npm（`internal/site/node_modules`）。

## 2026-03-04 补充：系统地址批量切换到 193

### planning
- 复用任务：`planning/05-20260303-192193网络波动掉线采集告警/`。
- 目标：将所有 `systems.host` 的 `192.168.192.*` 批量改为 `192.168.193.*`。

### implementation
- 执行数据库备份：`beszel_data/data.db.bak.20260304-171733.ip192to193`。
- 执行批量替换：
  - `UPDATE systems SET host=replace(host,'192.168.192.','192.168.193.') WHERE host LIKE '192.168.192.%';`
- 重启 Hub：`docker compose restart beszel`，等待轮询周期重连。

### completion
- 替换结果：`cnt_192: 11 -> 0`，`cnt_193: 2 -> 13`。
- 在线状态：`up/down` 从 `5/9` 改善到 `12/2`。
- 剩余离线：`15Xpro-wsl`、`SjH-OpenWrt`（需单机排障）。

### project_summary
- 地址治理后，监控链路已统一到 193 网段，绝大部分节点恢复在线。

### usage_guide
- 快速核验替换：
  - `sqlite3 beszel_data/data.db \"SELECT COUNT(*) FROM systems WHERE host LIKE '192.168.192.%';\"`
- 快速核验状态：
  - `sqlite3 beszel_data/data.db \"SELECT status, COUNT(*) FROM systems GROUP BY status;\"`

## 2026-03-04 补充：首页与记录页补齐 193 延迟展示

### planning
- 复用任务：`planning/05-20260303-192193网络波动掉线采集告警/`。
- 目标：修复“首页和记录页看不到 193 延迟”的展示缺口，并验证数据是否持续更新。

### implementation
- 首页修复：
  - 文件：`internal/site/src/components/systems-table/systems-table-columns.tsx`。
  - 新增 `ZT 193 Latency` 列，读取 `systems.info.z193 / z193_jitter / z193_status`，显示 `up/down/na` 状态与数值。
- 记录页修复：
  - 文件：`internal/site/src/components/routes/settings/alerts-history-data-table.tsx`。
  - 在 Alert History 页面新增 `ZT 193 Latency` 采样记录表，数据源 `system_stats.type='zt1m'`。
  - 增加对 `system_stats` 的实时订阅，新增采样会自动刷新列表。
- 类型补齐：
  - 文件：`internal/site/src/types.d.ts`。
  - 增加 `SystemInfo` 的 `z193` 相关字段与 `ZtLatencyRecord` 类型。

### completion
- 构建验证：`cd internal/site && npm run -s build` 通过。
- 部署验证：
  - `docker build -f internal/dockerfile_hub -t beszel:zt-latency-email .`
  - `docker compose up -d beszel`
  - `curl http://127.0.0.1:38005/api/health` 返回 `200`
- 数据更新验证（65 秒）：
  - 前：`count=966, max(created)=2026-03-04 09:49:12.644Z`
  - 后：`count=980, max(created)=2026-03-04 09:50:17.529Z`
- 结论：采集链路正常，问题确认为前端展示缺口，已修复。

### project_summary
- 193 延迟现在在三个层面可见：系统详情曲线、首页汇总列、记录页采样表。
- 运维可以在首页快速发现异常，在记录页回看采样细节，在详情页看趋势曲线。

### usage_guide
- 首页查看：`All Systems` 表中 `ZT 193 Latency` 列。
- 记录页查看：`Settings -> Alert History` 页面底部 `ZT 193 Latency` 表。
- 数据自检：
  - `sqlite3 beszel_data/data.db "SELECT COUNT(*), MAX(created) FROM system_stats WHERE type='zt1m';"`

### Git状态补充（首页/记录页193修复）
- 本地提交：`2b630395`（首页 + 记录页 193 延迟展示修复）。
- 远端推送：失败，`403 Permission to henrygd/beszel.git denied to l2ktech`。

## 2026-03-05 补充：原生图表“数据准备中”回归修复

### planning
- 复用任务：`planning/05-20260303-192193网络波动掉线采集告警/`。
- 目标：修复“历史页只见延迟，其他图表长期数据准备中”的回归问题。

### implementation
- 根因定位：
  - `zt1m` 持续写入；
  - Hub 原生 `system_stats`（尤其 `1m`）在多数系统停更。
- 后端修复：
  - 文件：`internal/hub/systems/system.go`。
  - 新增 `decodeWithTimeout`、`waitWithTimeout`。
  - 在 `fetchDataViaSSH` 中为 `cbor/json decode` 与 `session.Wait()` 增加超时保护，避免采集协程卡死。

### completion
- 编译验证：`go build ./internal/cmd/hub` 通过。
- 部署验证：
  - `docker build -f internal/dockerfile_hub -t beszel:zt-latency-email .`
  - `docker compose up -d beszel`
  - `curl http://127.0.0.1:38005/api/health` 返回 `200`
- 修复结果：
  - 12 台 `up` 系统均恢复 `1m` 新样本；
  - `zt1m` 继续稳定增长；
  - 原生图表不再长期停在“数据准备中”。

### project_summary
- 193 延迟采集与 Hub 原生资源采集链路已重新对齐。
- 当前状态：延迟图与 CPU/内存/磁盘等图表均可持续更新。

### usage_guide
- 快速检查原生采样：
  - `sqlite3 beszel_data/data.db "SELECT s.name, COUNT(ss.id), MAX(ss.created) FROM systems s LEFT JOIN system_stats ss ON ss.system=s.id AND ss.type='1m' GROUP BY s.id;"`
- 快速检查延迟采样：
  - `sqlite3 beszel_data/data.db "SELECT COUNT(*), MAX(created) FROM system_stats WHERE type='zt1m';"`

### Git状态补充（数据准备中回归修复）
- 本地提交：`259a24b4`（SSH 采集超时保护修复）。
- 本地提交：`01605673`（planning 提交记录）。
- 本地提交：`4579a881`（push 阻塞记录）。
- 远端推送：失败，`403 Permission to henrygd/beszel.git denied to l2ktech`。

## 2026-03-05 补充：二次“数据准备中”与两台 down 设备修复

### planning
- 复用任务：`planning/05-20260303-192193网络波动掉线采集告警/`。
- 目标：
  1. 修复二次出现的“正在收集足够的数据来显示”；
  2. 修复遗留 `down` 设备 `15Xpro-wsl`、`SjH-OpenWrt`。

### implementation
- 后端二次加固：
  - 文件：`internal/hub/systems/system.go`。
  - 将 `runSSHOperation` 升级为 `sessionTimeout + operationTimeout` 双超时模型。
  - 在超时时主动关闭 `session` 并触发重试，防止 `Shell/Encode` 阶段卡死。
- 设备修复：
  - `15Xpro-wsl`：通过 `192.168.193.17:22022` SSH 复核，`beszel-agent -l :45877` 监听正常，恢复稳定在线。
  - `SjH-OpenWrt`：通过 `rock5c` 跳板连接 `192.168.1.1:35622`，确认 agent 进程与 token 正常；因 `192.168.193.203` 网络控制面未下发配置，临时改用 `192.168.1.1:45876` 作为 Hub 连接地址恢复采集。

### completion
- 二次修复验证：
  - 重建并重启 Hub 后，85 秒复测 `1m` 记录 `155 -> 181`。
  - `zt1m` 持续增长，`max(created)` 连续前进。
- 设备状态验证：
  - `up/down` 从 `13/1` 收敛到 `14/0`。
  - `SjH-OpenWrt` 与 `15Xpro-wsl` 均出现新的 `1m` 样本写入。

### project_summary
- 原生资源图与 193 延迟图已恢复同频更新，不再出现长时间“数据准备中”。
- 两台历史离线设备已恢复在线；其中 `SjH` 目前以管理地址兜底在线。

### usage_guide
- 观察二次回归是否复发：
  - `sqlite3 beszel_data/data.db "SELECT type, COUNT(*), MAX(created) FROM system_stats GROUP BY type;"`
  - `sqlite3 beszel_data/data.db "SELECT name, status, updated FROM systems ORDER BY name;"`
- 重点核验设备：
  - `sqlite3 beszel_data/data.db "SELECT created, s.name FROM system_stats ss JOIN systems s ON s.id=ss.system WHERE ss.type='1m' AND s.name IN ('SjH-OpenWrt','15Xpro-wsl') ORDER BY created DESC LIMIT 20;"`

## 2026-03-08 UI 未更新 / 数据停更恢复

### 现象
- 用户反馈 Beszel 界面“看起来没更新”，很多数据停在几天前。
- 现场确认：容器仍在线，但 `system_stats.type in ('1m','zt1m')` 最新样本曾停在 `2026-03-05`。

### 根因
- `beszel_data/data.db` 已损坏，`PRAGMA quick_check` 报 `database disk image is malformed`。
- 同时确认 PocketBase 默认以 `journal_mode(WAL)` 连接 SQLite；在当前 `macOS + OrbStack + bind mount ./beszel_data:/beszel_data` 组合下，恢复库若继续以 WAL 打开，容易再次出现 malformed。

### 实施修复
- 离线恢复数据库：
  - `sqlite3 beszel_data/data.db ".recover" > /tmp/beszel-recover.sql`
  - `sqlite3 /tmp/beszel-recovered.db < /tmp/beszel-recover.sql`
- 新增 Hub SQLite pragma 配置：
  - 代码：`internal/cmd/hub/hub.go`
  - 环境变量：
    - `BESZEL_HUB_SQLITE_JOURNAL_MODE=DELETE`
    - `BESZEL_HUB_SQLITE_SYNCHRONOUS=FULL`
- 重建并重启 Hub：
  - `docker build -f internal/dockerfile_hub -t beszel:zt-latency-email .`
  - `docker compose up -d beszel`
- 数据库切换前保留备份：
  - `beszel_data/data.db.bak.20260308-081949.pre-recover`
  - `beszel_data/data.db.bak.20260308-082356.pre-delete-mode`
  - 原坏库：`beszel_data/data.db.corrupt.20260308-081949`

### 验证结果
- 当前 `journal_mode=delete`，`pragma quick_check=ok`。
- 85 秒复测：
  - `1m: 550 -> 568`
  - `zt1m: 13230 -> 13272`
- 最新样本时间推进到 `2026-03-08 08:25:30+08:00` 附近，说明原生资源采集与 193 延迟采集都已恢复。

### 使用说明补充
- 检查数据库完整性：
  - `sqlite3 beszel_data/data.db 'pragma quick_check;'`
- 检查 SQLite 日志模式：
  - `sqlite3 beszel_data/data.db 'pragma journal_mode;'`
- 检查最近样本是否推进：
  - `sqlite3 beszel_data/data.db "select type, max(created), count(*) from system_stats where type in ('1m','zt1m') group by type;"`
- 手动补跑 193 延迟采集：
  - `./scripts/zt_latency_sync.sh`

### 剩余问题
- `jetson` 当前仍为 `down`。
- `SjH-OpenWrt` 当前仍为 `paused`。
- 这两个属于设备侧历史状态，不影响本次 Beszel 主链路恢复。

## 2026-03-08 补充：193 列空白与首页系统列表展示修复

### 根因
- `zt_latency_sync.sh` 的 193 探测本身正常，但 Hub 在保存系统状态时直接 `Set("info", data.Info)`，把脚本注入的 `z193*` 字段覆盖掉了。
- 首页只显示约 7 个系统并不是条目不够，而是表格容器做了固定最大高度 + 虚拟滚动。

### 修复
- 后端：`internal/hub/systems/system.go`
  - 保存系统记录时改为“保留 `systems.info` 中非原生字段”，避免自定义 `z193/z193_jitter/z193_status/zt_probe_ts` 被冲掉。
- 前端：`internal/site/src/components/systems-table/systems-table.tsx`
  - 当系统数 `<=100` 时直接全量渲染，不再限制为一个只显示约 7 行的滚动窗口。
- 部署：
  - `cd internal/site && npm run -s build`
  - `docker build -f internal/dockerfile_hub -t beszel:zt-latency-email .`
  - `docker compose up -d beszel`

### 验证
- `systems.info.z193*` 已恢复，例如：`macmini=0ms`、`gw-4060=1ms`、`thinkbook16=12ms`。
- 当前采样：
  - `1m|2026-03-08 00:44:45.306Z|803`
  - `zt1m|2026-03-08 00:45:08.817Z|13524`
- 前台根页面已切到新构建资源：`/assets/index-DVLGcacx.js`。

### 使用说明
- 如果浏览器仍看到旧页面，刷新一次根页面即可；由于静态资源 hash 已变，更彻底的方式是强刷。


## 2026-03-08 补充：Status 开关联动 zT 钉钉通知

### 根因
- UI 小铃铛中的 `Status` 开关原本只控制 Beszel 内建状态告警。
- `zt_latency_sync.sh` 走的是独立钉钉 webhook 链路，因此即使界面里关掉 `Status`，脚本仍会继续给 `down` 设备发 zT 通知。

### 修复
- 文件：`scripts/zt_latency_sync.sh`
- 发送 `offline/recovery/jitter` 之前，增加对 `alerts` 表的检查：
  - 当前系统存在 `name='Status'` 记录 -> 允许发送 zT 通知
  - 当前系统不存在 `name='Status'` 记录 -> 静默，不发送 zT 通知
- 保留 `z193` 采样本身，不影响首页列、图表和历史记录。

### 验证
- `jetson` / `SjH-OpenWrt` 在 `Status` 关闭状态下，执行脚本后 `z193_last_offline_alert_ts` 不再更新。
- 日志中无新的 `alert event` / `dingtalk send`，说明脚本通知已被成功静默。

### 使用说明
- 现在界面小铃铛中的 `Status` 开关 = 该设备 zT 钉钉通知开关。
- 关闭 `Status`：
  - 继续采样 `z193`
  - 不再发送 zT 离线/恢复/抖动通知
- 打开 `Status`：恢复该设备的 zT 钉钉通知。


## 2026-03-08 补充：MacBook 改为 WebSocket 连接

### 现象
- `MacBook` 在 SSH 采集模式下会偶发显示红色短暂掉线，最新一次持续约 `48s` 后自动恢复。
- 同一时间 `z193` 探测也会短暂变成 `down`，说明不是纯前端误报。

### 处理
- 连接 `MacBook(192.168.193.18)` 检查 launchd 与 agent。
- 确认当前为 SSH 模式（`ct=1`），然后改为 WebSocket 优先：
  - 在 `~/Library/LaunchAgents/com.beszel.agent.plist` 中保留 `KEY` / `LISTEN`
  - 增加 `HUB_URL=http://192.168.193.13:38005`
  - 刷新专属 `TOKEN` 后写入 launchd
- 重启 `com.beszel.agent`，等待回连。

### 结果
- Agent 日志出现：`WebSocket connected host=192.168.193.13:38005`。
- Hub 中 `macbook.info.ct` 已变为 `2`，表示当前主连接模式为 WebSocket。
- 切换后至少 75 秒内未新增新的 `Status` 掉线事件。

### 结论
- 之前的短暂红色掉线，更像是 MacBook 在 SSH/ZeroTier 轮询链路上的瞬时不可达。
- 改为 WebSocket 后，已显著降低该现象再次出现的概率。


## 2026-03-08 补充：DS224 群晖接入 Beszel（WebSocket）

### 前提
- 设备：Synology DS224+
- 可用 SSH：`wzy@192.168.1.100:35622`
- 认证：密码 + `sudo`
- Docker / Container Manager：已安装可用

### 处理
- 在 Hub 中为 `ds224` 预创建系统记录与专属 token。
- 在群晖上创建数据目录：`/volume3/docker/beszel-agent`。
- 启动容器：
  - 镜像：`henrygd/beszel-agent:latest`
  - 模式：`--network host`
  - 挂载：
    - `/var/run/docker.sock:/var/run/docker.sock:ro`
    - `/volume3/docker/beszel-agent:/var/lib/beszel-agent`
  - 环境变量：`KEY`、`TOKEN`、`HUB_URL`、`LISTEN`
- 其中 `HUB_URL` 设为 `http://192.168.1.4:38005`，让群晖直接以 WebSocket 常连接方式接 Hub。

### 验证
- 群晖容器日志：`WebSocket connected host=192.168.1.4:38005`
- Hub 数据库状态：
  - `ds224.status = up`
  - `ds224.info.ct = 2`
  - `ds224.info.v = 0.18.4`
- 指纹已落库，说明该系统已完成首次 WebSocket 注册。

### 结论
- DS224 已成功加入 Beszel 采集。
- 当前采集方式为 WebSocket 常连接，不是 SSH 轮询。
