# 2026-02-10-01《Beszel迁移部署与密钥记录》

## 一、当前生产信息（已生效）

### 1. 新 Hub（主）
- 位置：`Macmini`
- Hub 容器：`beszel`
- Hub 本地地址：`http://127.0.0.1:8090`
- 对外地址（HTTPS）：`https://beszel.l2k.tech:38005`
- 外网转发：`BE6500:38005 -> 192.168.1.4:38005`

### 2. 账号与密码
- 登录邮箱：`442333521@qq.com`
- 当前密码：`!Wanzgeyu166!@#`

### 3. 关键密钥（当前有效）
- Hub 公钥（Agent 的 KEY 必须使用这个）：

```text
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAINELUHo1RjGRB8vL9QrshyHCvtc0Qwi8bXgMJf370NpH
```

## 二、本次迁移与修复的完整步骤

### 步骤 A：迁移 Hub 到 Macmini
1. 保留 Macmini 上 Hub 数据目录：`/Users/wzy/40-Projects/55-Beszel/beszel_data`
2. Hub 启动并验证：`http://127.0.0.1:8090`
3. Nginx 增加站点：`beszel.l2k.tech:38005 -> 127.0.0.1:8090`
4. BE6500 增加 DNAT：`38005 -> 192.168.1.4:38005`

### 步骤 B：证书与域名
1. 证书通过 `acme.sh + DNSPod API` 签发
2. 域名记录：`beszel.l2k.tech`
3. 证书目录：`/Users/wzy/projects/dsm-proxy/certs/l2k-multi/`

### 步骤 C：Macmini Agent 改为主机服务（采集真实主机资源）
1. 安装二进制：`/Users/wzy/bin/beszel-agent`
2. 创建/更新 launchd：`/Users/wzy/Library/LaunchAgents/com.beszel.agent.plist`
3. 仅保留以下环境变量：
   - `KEY=<Hub公钥>`
   - `PORT=45876`
4. 去掉错误变量：
   - 不使用旧的错误 `TOKEN`
   - SSH 模式无需依赖 `HUB_URL`
5. 重启服务后验证 45876 监听

### 步骤 D：排障结论（为什么有设备自动接入、有设备失败）

#### 自动接入成功的原因
- 这些设备上的 agent 原本就是有效配置（KEY 正确、端口监听正常）
- Hub 能通过其 host:port（含 ZeroTier 地址）连通

#### `top` 与 `thinkbook-wsl` 之前失败的根因
1. `top-rustdesk-wzy`
   - 存在错误 drop-in 覆盖：`key.conf / hub.conf`
   - `hub.conf` 指向旧 Hub：`http://192.168.192.10:38005`
   - `key.conf` 覆盖为错误 KEY，导致日志 `Invalid SSH key`
   - 修复：删除两个 drop-in，重启 `beszel-agent`

2. `thinkbook16-wsl`
   - 进程在跑但 45876 未正常对外监听
   - 还残留脚本尝试连接旧 Hub
   - 修复：杀旧进程，用正确 KEY+PORT 重新启动，确认 45876 监听

### 步骤 E：旧 Hub 清理（gw-4060）
1. 旧 Hub 容器 `beszel` 已删除
2. 按当前确认：不再使用 gw-4060 容器版 `beszel-agent`
3. gw-4060 保留主机版 agent 接入新 Hub 即可

## 三、关键配置文件清单

### Macmini
- Hub compose：`/Users/wzy/40-Projects/55-Beszel/docker-compose.yml`
- Hub 数据：`/Users/wzy/40-Projects/55-Beszel/beszel_data/data.db`
- Nginx 配置：`/Users/wzy/projects/dsm-proxy/nginx/conf.d/dsm.conf`
- LaunchAgent：`/Users/wzy/Library/LaunchAgents/com.beszel.agent.plist`

### 路由器（BE6500）
- 防火墙 DNAT：`WAN 38005 -> 192.168.1.4:38005`

## 四、复用模板（给其他设备）

### Linux systemd（推荐）
```ini
[Service]
Environment="KEY=ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAINELUHo1RjGRB8vL9QrshyHCvtc0Qwi8bXgMJf370NpH"
Environment="LISTEN=45876"
ExecStart=/opt/beszel-agent/beszel-agent
```

### 手动启动（二进制）
```bash
KEY="ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAINELUHo1RjGRB8vL9QrshyHCvtc0Qwi8bXgMJf370NpH" \
PORT=45876 \
./beszel-agent
```

## 五、当前状态快照
- 新 Hub 系统总数：16
- 已确认迁移一致：16/16（新旧名称、host、port 对齐）
- `top-rustdesk-wzy`：已修复
- `thinkbook16-wsl`：已修复

---

> 安全提示：本文包含真实密码与密钥，仅保存在私有目录使用，不要外发。
