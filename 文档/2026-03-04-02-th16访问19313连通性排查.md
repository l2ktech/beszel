# 2026-03-04 th16 访问 192.168.193.13:38005 连通性排查

## planning
- 复用任务：`planning/05-20260303-192193网络波动掉线采集告警/`
- 查重结论：Top1 为当前任务目录（相似度 93%），按规则复用，不新建目录。
- 目标：确认 `th16` 无法访问 `http://192.168.193.13:38005/` 的根因属于服务端、网段侧还是 th16 本机侧。

## implementation
- 本次无代码实现变更，执行连通性排查与交叉验证：
  1. 本机测试目标地址：`ping`、`nc`、`curl -I http://192.168.193.13:38005/`
  2. 本机监听确认：`lsof -iTCP:38005 -sTCP:LISTEN -nP`
  3. 容器映射确认：`docker ps` 检查 `beszel` 端口映射
  4. 交叉节点验证：SSH 到 `192.168.193.10:35622`，反向 `curl` 到 `192.168.193.13:38005`
  5. 网段在线性抽查：`192.168.193.*` 关键地址 ICMP 探测
  6. SSH MCP 可用性核查：`win-cli` MCP 接口调用

## completion
- 结论：
  - `192.168.193.13:38005` 当前可用，返回 `HTTP 200`。
  - `192.168.193.10` 到 `.13:38005` 也可用，说明 193 网段横向通信正常。
  - 问题更可能在 `th16` 本机（代理、浏览器策略、路由、DNS 缓存或本机防火墙），而非 `.13` 服务端。
- 补充观察：
  - `win-cli` SSH MCP 当前不可用（response body 解码错误）。
  - `192.168.193.16:35622` 可连，但现有密钥在该主机鉴权失败，无法直接远程执行 th16 本机命令。

## project_summary
- 本项目用于 Beszel 及周边网络/监控链路运维。本次任务属于 193 网段访问故障的现场诊断，目标是快速判定故障边界并给出可执行排查路径。

## usage_guide
- 建议在 th16 本机执行以下命令做最终定位：
  1. `curl -I --max-time 5 http://192.168.193.13:38005/`
  2. `nc -vz -w 3 192.168.193.13 38005`
  3. `ip route get 192.168.193.13`
  4. `env | rg -i 'http_proxy|https_proxy|all_proxy|no_proxy'`
  5. `curl -I --noproxy '*' --max-time 5 http://192.168.193.13:38005/`
- 判定建议：
  - 若 `--noproxy` 成功、默认失败：优先修正代理变量或浏览器代理规则。
  - 若 `nc` 失败但同网段其他主机可达：优先检查 th16 本机防火墙与路由策略。
