# 进度日志

## 会话 2026-03-03
### 完成
- [x] 完成 planning 查重并新建任务目录（符合命名规范）
- [x] 完成本机侧故障复现与证据采集
- [x] 完成 Grok 检索并归纳修复路径
- [x] 使用 `~/.ssh/id_rsa_github` 登录 friendlyWrt 并执行现场修复
- [x] 清理主实例残留网络（移除 `c329...` / `5cb...` from main）
- [x] 主实例防火墙放行（`zerotier.top.fw_allow_input=1`）
- [x] 主实例身份旋转试验并回滚（新ID `ACCESS_DENIED`，恢复旧ID）
- [x] 恢复双入口可管理性（`192.168.193.15` 可 SSH）
- [x] 恢复 `zerotier-self` 开机自启为原状态（避免重启后丢失 193 管理入口）
- [x] 完成自建 ZeroTier 成员授权：`8b7d0422ba` 从 `authorized=false` 改为 `true`

### 问题与解决
- **问题**：`ssh root@192.168.193.15` 无可用认证（publickey/password 均未通过）。
- **解决**：改用 `~/.ssh/id_rsa_github` 成功接管设备。

- **问题**：主实例更换身份后加入官方网 `565...` 出现 `ACCESS_DENIED`。
- **解决**：已立即回滚到旧身份 `8b7d0422ba`，恢复 `565... OK`。

- **问题**：虽已修复设备侧明显配置缺陷，但 `192.168.192.15` 仍不可从部分节点访问。
- **解决**：判定阻塞点在官方 ZeroTier 控制面策略/授权，需在 `565799d8f61f7c2d` 控制台处理成员规则后复测。

- **问题**：用户要求 SSH 到 `txy` 查看自建 ZeroTier 并授权，但 `txy` 的 SSH 端口均拒绝连接。
- **解决**：改走 Zero-UI API（`192.168.192.1:4000`）完成同等授权动作，结果已确认生效。

## 会话 2026-03-03（15:40 CST）
### 完成
- [x] 复用既有 planning（同故障域，网络连通性问题）
- [x] 完成 `192/193` 全量节点可达矩阵复测
- [x] 完成本机 ZeroTier 状态复核（双网络均 `OK`，本机地址正常）
- [x] 完成 friendlyWrt 侧交叉验证（与本机同分布“部分可达”）
- [x] 完成根因归纳：官方 `192` 网络控制面策略/成员状态问题为主因

### 问题与解决
- **问题**：用户反馈“192 和 193 网络都不通”。
- **解决**：实测结论为“193 基本通、192 部分不通”，非双网全断。已锁定排查重心到网络 `565799d8f61f7c2d` 的授权与规则。

## 会话 2026-03-05（friendlyWrt 下挂设备访问 193 修复）
### 完成
- [x] 复用 planning（Top1 相似度 94%，满足复用阈值 >=40%）
- [x] 完成 Grok 优先检索并确认修复策略（zone + forwarding + masquerade）
- [x] SSH 登录 friendlyWrt，定位根因为缺失 `br-lan -> ztdfilglme` 的 NAT/转发链路
- [x] 新增防火墙 `zt193` zone（`device=ztdfilglme`、`masq=1`）
- [x] 新增双向 forwarding（`lan->zt193`、`zt193->lan`）
- [x] 重启 `zerotier-self` 并恢复 `ztdfilglme(192.168.193.15/24)`
- [x] 路由器侧回归通过：`192.168.193.13/.10/.1/.18` 可达
- [x] 完成 MCP 钉钉任务通知发送

### 问题与解决
- **问题**：首次尝试将 `ztdfilglme` 绑定为 `network.zt193` 并 `network reload` 后，`192.168.193.15` 入口超时。
- **解决**：通过 `192.168.192.15` 备用入口回连，回滚 netifd 绑定，改为 firewall `device` 方案并恢复服务。

- **问题**：无法直接在下挂终端执行命令验证 NAT 命中计数。
- **解决**：先完成路由器侧可用性验证与持久化配置，待用户现场终端复测闭环。
